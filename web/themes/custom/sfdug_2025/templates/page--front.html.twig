{#
/**
 * @file
 * SFDUG 2025 front page template.
 */
#}
<div class="layout-container">
  <header role="banner" class="site-header">
    <div class="container">
      <div class="site-header__inner">
        {{ page.header }}
        {% if page.primary_menu %}
          <nav class="primary-menu">
            {{ page.primary_menu }}
          </nav>
        {% endif %}
      </div>
    </div>
  </header>

  <main role="main" class="main-content">
    <div class="container">
      {% if page.highlighted %}
        <div class="highlighted">{{ page.highlighted }}</div>
      {% endif %}

      <section class="hero">
        <h2 class="hero__title">Twenty Years of Bay Area Drupal</h2>
        <p class="hero__subtitle">Est. April 2006</p>
        <div class="hero__story">
          <p>In April 2006, Zack Rosen and Gregory Heller organized the first Drupal Camp SF — a two-day training at Compumentor's offices with Jeff Robbins from Lullabot. It sold out in five days. Half the attendees flew in from out of state.</p>
          <p><strong>That was the beginning. We're still here.</strong></p>
        </div>
      </section>

      <section class="meetings-archive">
        <h2 class="section-title">Meeting Archive</h2>
        {{ page.content }}
      </section>
    </div>
  </main>

  {% if page.footer %}
    <footer role="contentinfo" class="site-footer">
      <div class="container">
        {{ page.footer }}
        <div class="footer-credits">
          <p>Part of the <a href="https://www.badcamp.org">BADCamp</a> family</p>
        </div>
      </div>
    </footer>
  {% endif %}
</div>
